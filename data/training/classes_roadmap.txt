# IRLSAFETY+ YOLO classes — use exact names in LabelImg
#
# v0.7 detection model (train locally)
license_plate
street_sign
shipping_label
id_document

# shipping_label covers: UPS, FedEx, Amazon, USPS, DHL, OnTrac, mail parcels, padded envelopes
# id_document covers: driver license, state ID, passport page held to camera

# Optional future classes
payment_card
face

# Legacy aliases (migrated on load in plugin settings)
document
face