name: Pull Request (disabled)

# PR builds disabled to avoid failed-job email noise.
# Re-enable check-format or build-project here when OBS CI is configured.

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: echo "PR CI disabled. Use Android APK Release workflow for mobile builds."